local gameobjects = require "main.modules.gameobjects"

function init(self)
	local player = gameobjects.get(hash("player"))
	msg.post("camera", "follow", { 
		target = player, 
		lerp = 0.8, 
		horizontal = true, 
		vertical = true, 
		immediate = true 
	})
end
